/* ============================================================
   config.js — الإعدادات العامة للتطبيق
   ============================================================ */
(function () {
  "use strict";

  window.APP_CONFIG = {
    version: "3.0.0",

    // مفتاح التخزين المحلي — غيّره إذا أردت فصل نسخة جديدة
    storageKey: "computer-adventure-v3",

    // الشخصيات المتاحة للّاعب
    avatars: ["🤖", "🧑‍🚀", "🧑‍💻", "🦸", "🧑‍🔬", "🎮"],

    // اللغات المدعومة (الترتيب مهم لظهورها في شاشة البداية)
    supportedLangs: ["ar", "he"],

    // اللغة الافتراضية (تُستخدم إذا لم يختر المستخدم)
    defaultLang: "ar",

    // بريد الدعم الفني — ✏️ غيّره إلى بريدك الحقيقي
    supportEmail: "lotfe.shekh.zeed.1@gmail.com",

    // بيانات عامة
    minAge: 10,
    maxAge: 18,

    // ⚙️ سلوك النظام
    enablePrayerScreen: true,   // ← اجعلها false لإخفاء قسم الصلاة كاملًا
    enableConfetti: false,      // ← فعّلها إذا أضفت مكتبة confetti لاحقًا
  };
})();
